[TASK]
Recommend a maximum of 3 logical next materials for this student to study.
Always return between 1 and 3 items — never return an empty array.

---
[STUDENT DATA]

Available (pick from here if In Progress is empty, or to fill up to 3 slots):
{% if available %}
{% for b in available %}
- bundle_id: {{ b.bundle_id }} | mapel_label: {{ b.mapel_label }} | elemen_label: {{ b.elemen_label }} | materi: {{ b.materi }} | atp: {{ b.atp }}
{% endfor %}
{% else %}
(none)
{% endif %}

In Progress (study these FIRST — always include if non-empty):
{% if in_progress %}
{% for b in in_progress %}
- bundle_id: {{ b.bundle_id }} | materi: {{ b.materi }}
{% endfor %}
{% else %}
(none)
{% endif %}

Completed (NEVER recommend these):
{% if complete %}
{% for b in complete %}
- bundle_id: {{ b.bundle_id }} | materi: {{ b.materi }}
{% endfor %}
{% else %}
(none)
{% endif %}

---
[SELECTION RULES]
1. Pick from In Progress first. Then fill remaining slots (up to 3 total) from Available.
2. Never recommend bundle_ids that appear in Completed.
3. If Available has only 1 or 2 items, recommend ALL of them — do not skip any.
4. For materi field: if the source shows null/empty/"None"/"N/A", copy the elemen_label value into materi.
5. For alasan: write 1–2 sentences in Indonesian that are specific to the material. For In Progress items, mention continuing toward  completion. For Available items, briefly explain WHY this material  matters based on its ATP learning objectives — make it motivating and concrete, not generic.
6. All bundle_id, mapel_label, elemen_label, materi values MUST be copied exactly from the data above — no rewording.

---
[OUTPUT FORMAT]
Return exactly this structure and nothing else:
{
  "rekomendasi": [
    {
      "bundle_id": "string",
      "mapel_label": "string",
      "elemen_label": "string",
      "materi": "string",
      "alasan": "string"
    }
  ]
}